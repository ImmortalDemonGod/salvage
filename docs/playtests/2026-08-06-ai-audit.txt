The interesting pattern: the most valuable things those games have are mostly things SALVAGE's own docs already name as spec'd-but-unbuilt or open — the comparisons mainly tell you which of your open items matter most. Ranked by fit against the prototype's own goals and its "state space is a budget" constraint:**1. Into the Breach's turn undo.** ItB lets you freely reposition before committing, and reset the whole turn once — and that's philosophically *consistent* with determinism, not a cheat: with no hidden rolls, undo can't be used to fish for outcomes, it only lets you finish thinking. SALVAGE currently punishes a misclick with irreversibly spent Air, and the playtest transcript is full of interface fumbles ("now it's F, I thought it was space"). This is also nearly free to build: `Combat.clone()` already exists for the search, so "snapshot at turn start, restore on U" is a few lines and adds zero state-space cost. Highest value per unit of work on the list.**2. Monster Hunter's breaks that *change* the monster, not just de-fang it.** In MH a broken part makes the monster limp, rage, swap attacks. In SALVAGE a break only ever *removes* threat — the board monotonically relaxes. This is exactly the repo's own top open item ("2.4's boss whose station map changes mid-fight... the prototype does not answer it"). A deterministic version — breaking the claw makes the wounded tail gain a wider arc, announced like everything else — makes breaks a real decision (which threat do you want to *create*?) instead of a checklist, and answers the escalation question the prototype was built to ask.**3. Into the Breach's displacement.** ItB's core verb isn't damage, it's *pushing* — you answer attacks by moving the attacker. SPEC 2.9 already assigns Scuba exactly this verb ("shoves an enemy part") and the reversals log admits it was never built; Axe Kick shipped as a plain 2-damage hit. This matters because the docs' own analysis (INPUTS 3.6) says that with small deterministic numbers, variety *must* live in effects and positioning, not damage values — and displacement is the single biggest source of that variety in the genre. Moderate state-space cost, but it's the one that makes movement offensive rather than just evasive.**4. Into the Breach's "protect the thing" objectives.** ItB's tension comes from defending buildings while killing bugs — offense and defense compete for the same actions. Every SALVAGE fight is pure offense ("break every limb"), and the salvage itself only exists as a defeat counter. Put the salvage *on the board* — a crate at a station with its own small HP that announced attacks can hit — and you get the defend-vs-attack dilemma, a diegetic reason the fights are about salvage at all, and a new use for safe stations. Deterministic, readable, one new HP bar.**5. Slay the Spire's build choice between fights.** The run grants abilities on a fixed ladder; StS's identity comes from *choosing* your kit. The cheapest version is already latent in the design: the drum's condition is meant to be a fitted, swappable mode — let the boat offer a pick-one-of-two. That simultaneously fixes "the boat is two text panels with no job" (their open item) with zero RNG. The cost is verification, not code: the bands must then measure both configurations, or you recreate the "measured a configuration no player ever meets" bug they already fixed once.**6. Zelda's environment-in-combat.** SPEC 2.12 notes it themselves as free escalation: "a room whose water level changes is a room whose stations change." A fight where a valve on the board floods/drains stations mid-combat merges the two already-taught systems into the boss format — this is basically what item 2 needs anyway, delivered through the puzzle grammar instead of anatomy.One honorable mention with a caveat: **Darkest Dungeon's position-gated abilities** (moves usable only from certain slots) would deepen "the limb is the position," and the drum-from-BACKLINE proves the pattern reads — but it costs teach-ladder budget, and the ladder is already tight.And worth saying explicitly: the rest of what those games have, SALVAGE is right to *not* take. DD's variance-as-drama, per-unit action points, second resources, StS's randomized rewards — the docs litigated each of these against measured evidence (stamina never bound; dice were never the fun part), and importing them would undo the one thing this prototype does better than its ancestors: every outcome is countable before you commit. If you want, I can turn the top three or four of these into spec-style proposals (rule text + the gate that would verify each) on a branch.A new human playtest recording — this is exactly the material for G5-HUMAN, the one gate the repo says only a human can close. I'll analyze it the way the repo's own method demands: read the full transcript exhaustively (PROGRESS.md's hardest-won lesson — "my summary of the evidence is not the evidence," the first playtest's second read found the item the player quit over), then classify every finding against the defect-class checklist and the five questions in PLAYTEST.md. Let me extract it first.The Otter summary adds nothing beyond the transcript, so the analysis below is grounded in the transcript itself, read line by line against the repo's own defect-class checklist, its gate ledger, and PLAYTEST.md's five questions.# Playtest 2 analysis — Aug 6, self-test by Miguel, quit at dive 8 of 9## The verdict first**G5-HUMAN just got measured, and it is RED.** 25 minutes (against a ten-minute target), abandoned mid-`deep1` after a diver went down, closing line: *"the Tide Song combat was fun. This one is just tedious, and I feel like I'm being punished for playing it."* That inverts the project's founding premise — Prototype 2 exists because TIDESONG's *combat model* needed replacing, and the first human verdict says the old combat was more fun than the new one. Every machine gate was green for this entire session, which is standing rule 26 ("a gate that cannot see the screen cannot tell you the game is ready") firing for the third time.## The most damning evidence is what's absentIn 25 minutes of think-aloud, the player **never once mentions**: the telegraph arcs or their damage numbers, reading a limb (`A`/traits/brittle/leaking), the drum's shutdown as a deliberate tool, air tiers (strained/desperate), or the umbilical rule as a *rule* (only as an inexplicable punishment: "now I have to cut my airline"). Those five systems are the design — determinism, information, counterplay. The dominance search proves each is uniquely optimal *somewhere*; the session proves none of them ever *reached the player*. The stated strategy that emerged instead: *"touch a player, move it to a place, tap space, click enter every now and again — the game was basically over"*, and by the dredge, *"use one player and click space a bunch of times, as seems to be the dominant strategy."*## Findings, classifiedStatus: **NEW** = first appearance; **KNOWN** = already logged open (G6/G10 lists); **BELOW-THRESHOLD** = a fix for this shipped after playtest 1 and still doesn't land (the INPUTS.md "perception threshold" class, now measured a third time).| # | Evidence (transcript) | Class | Status ||---|---|---|---|| 1 | "There's no way to just strategize… I have to play around with the keys, but that wastes my turn. You've moved now." | no undo / experimentation punished | **NEW** — and it independently confirms the Into-the-Breach undo item from my earlier analysis || 2 | "Why can't I just play the game by clicking? I also have to click AND use the keys." | input model | **NEW as a defect with a concrete hole**: `_click()` has no mouse path to ability slot 1, analyze, or end-turn — `F`, `A`, `ENTER` are keyboard-only, so mouse-first play is *impossible*, not just unsupported || 3 | "Which diver is controlled with the numbers? Is scuba one?… that moved the scuba diver… I have no idea which diver is which" (recurs 4×, all the way to the quit) | selection legibility | **NEW** — sprites carry name+HP but not their *number*; the selected marker is a `>` on a card and a subtle ring; clicking a diver's body moves/attacks instead of selecting || 4 | "80% of the screen is nothing but a panel" (measured ~61% by my count of the panel geometry — his point stands) | HUD real estate | KNOWN in weaker form (G6 MED "composition uses only the right 40%"), now escalated by a human || 5 | "How can I SHOW this rather than SAY this?… now it's just permanently on my screen forever" (legend, "break every limb," refusal lines) | teach-by-text vs teach-by-play | **NEW as a structural critique** — the prompt band was playtest 1's *fix*; this session rejects the medium itself: one-time contextual callouts at first relevance, then gone || 6 | "Is every animation just a slight bob?… the enemy is just floating there. Nothing happens" | motion readability | **BELOW-THRESHOLD** — lunge/bolt/recoil/shake shipped after playtest 1; still reads as nothing. Third recurrence of this exact class across two projects || 7 | "I killed it. No winning screen, just directly to another puzzle… transitions clearly not planned" / "the transition didn't last very long… don't know what it was supposed to do" | win/transition feedback | **BELOW-THRESHOLD** — the kill banner rides the 1.7 s descent overlay (playtest 1's fix) and is not perceived || 8 | "Prototype one comes down with you now. They were already on the rig." (both boats; Prototype1 also *fights* in fight1 before being introduced at boat1) | continuity | **NEW** — `_draw_rig()` always draws all three divers on deck, and the R7 ladder order means both "introductions" happen after the fact || 9 | "It eats three lines of air every time it **moves** or swings" — read aloud, "I don't know what that means" | copy contradicts sim | **NEW hard bug**: `beats.gd` boat2 copy says moving costs 3; `MOVE_COST` is flat 1. The same contradiction was fixed on the *cards* (G10 round); the story copy kept it || 10 | "Was I supposed to be saving that guy? Did I bring back a component? Why did I even go down there?" | goal feedback | **NEW** — dives never state or pay off an objective; the salvage never exists on screen. Matches SPEC's open "boat has no job / no NPC / no wants" cluster || 11 | "I press F, and nothing happens" (on the boat scene that just introduced F) | teaching moment unusable | **NEW** — the reward beat *tells* about the second ability but you cannot try it there || 12 | "Why doesn't the water flow?… I could have figured this out… had there been some actual reason, instead of all this text" | puzzle causality | **NEW** — lock 2's *state* is visible (their thesis) but its *dynamics* aren't: no visible flow through the crossover, no visible mechanism for "B must stay dry" || 13 | Fumbled selection → empty-water swing → "now I have to cut my airline" (felt arbitrary) | umbilical rule | SPEC's own flagged A1 risk manifesting: the vacate rule punishes UI fumbles, which reads as unfairness, not tension || 14 | "Why does it say prototype one and not a name?" | naming | KNOWN (G6 MED, README known-list) || 15 | "The character models look different from what Glass_Goat gave us… the FBX should already be in the repo" | art pipeline expectation | **NEW decision item** — the FBX *is* in the repo and deliberately unused (code-drawn brief); either render the rig or state the choice loudly, because even the informed operator expected the rig || 16 | Opening "text box isn't centered" | layout polish | NEW, minor — the box is centered; the left-aligned ragged text in a 900px panel reads off-center || 17 | Positives: music noticed; "audio feedback was a good idea"; valve clicking "okay, cool"; opening copy "less weird than last time" | — | keep |## PLAYTEST.md's five questions, answered by this session1. **Did you know what to do?** Only by reading the prompt band, and he resented the medium the whole way. Direction exists; it is delivered in the one form he rejects.2. **Could you tell what the enemy was about to do?** No evidence he ever could — the telegraph is never mentioned. He learned he was hit from damage numbers.3. **Did moving ever feel worth it?** He moved when told to. Movement as a *decision* never appears; experimenting with it "wastes my turn" (finding 1).4. **Where were you bored?** "Tedious" by dive 3 (fight1); quit at dive 8.5. **What did the locks want?** Lock 1: solved by clicking around, fine. Lock 2: "a test of can you read instructions" — mechanics understood only via text, causality never.## The through-line diagnosisThe findings chain into one causal story: **selection friction (3) + no undo (1) + keyboard-only gaps (2) make multi-diver play cognitively expensive → the cheapest viable line is one-diver space-mashing → mashing clears fights because the pinned band *guarantees* a casual player wins 55–90% → so the depth (telegraph planning, traits, shutdown, composition) is never needed, never engaged, and never even noticed → "tedious, punished for playing."** The G3 band and the G4 dominance proof are both green and both beside the point: G4 proves depth *exists*, G3 proves mash *suffices*, and the UI prices engaging the depth higher than ignoring it. That last step is a design fork the gates can't rule on — either some fight must *fail* the masher (an Into-the-Breach-style forcing puzzle, which contradicts the current casual-win band), or depth stays optional and must be made *cheaper to use* than to ignore. That's a rule-7 escalation: it needs your ruling, not a silent retune.Two session-level rulings you made mid-transcript that should be logged as decisions: **mouse-first input** ("we're removing keyboard controls entirely… you can keep them, I guess, or rework them"), and **show-don't-tell teaching** (one-time contextual callouts replacing the permanent legend/help text).## What this means for the harness (rule 24 candidates)- **A mouse-door check**: `door.gd` proves every action has a *key*; nothing proves every action has a *click path*. Finding 2 was sitting exactly in that blind spot and is machine-checkable today.- **A HUD-area budget** in `layout.gd`: sum visible panel area vs viewport, assert the board band keeps a floor. Finding 4 becomes a number instead of an impression.- **Copy-claims audit**: finding 9 (boat2's "moves or swings" vs `MOVE_COST=1`) is the string-coupling class again — at minimum fix the line; the cards were already fixed once for the identical claim.- **The perception-threshold class stays human**: stills can't judge motion (G10 reads stills), so motion/win-feedback needs cold reads of *captured film* (`CAPTURE_ALL=1` exists for exactly this), reviewed by someone who doesn't know what changed.One honest caveat for the record: this was the operator playing, not a blind tester — by the repo's definition it closes "a person who did not build it played it" (the run built it), but it's an informed-critic lens, and the blind-tester round PLAYTEST.md was written for hasn't happened. Nothing in this transcript makes that round less necessary; it makes it more urgent, because several findings (selection, mash-dominance) will hit a cold player harder.Want me to write this up into the repo (e.g., `docs/PLAYTEST2.md` findings table + BACKLOG lines, per standing rule 9) on the PR branch, and/or start fixing the mechanical subset — the mouse paths, the boat2 copy contradiction, the continuity of who's on the rig, and the sprite numbering — while the design forks (mash-dominance ruling, teach-by-play rework, HUD rebuild) wait for your call?